import IntroLab from '@/components/intro/IntroLab';

export const metadata = {
  title: 'Lab — abertura CUCA.',
  robots: { index: false, follow: false },
};

export default function Page() {
  return <IntroLab />;
}
